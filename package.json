{
  "name": "timeflow",
  "version": "1.0.0",
  "description": "TimeFlow — Seguimiento de tiempo personal",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0"
  },
  "dependencies": {
    "chart.js": "^4.4.1",
    "firebase": "^10.12.0"
  }
}
